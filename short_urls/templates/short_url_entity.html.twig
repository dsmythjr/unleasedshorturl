{#
/**
 * @file short_url_entity.html.twig
 * Default theme implementation to present Short URL data.
 *
 * This template is used when viewing Short URL pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_short_url_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('short_url_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
